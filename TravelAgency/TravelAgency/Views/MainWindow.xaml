<Window x:Class="TravelAgency.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:TravelAgency.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="600" 
        Width="815" 
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <viewModel:MainWindowViewModel x:Key="MainWindowViewModel"></viewModel:MainWindowViewModel>
    </Window.Resources>

    <Grid DataContext="{Binding Source={StaticResource MainWindowViewModel}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Command="{Binding OpenBookingViewCommand}"
                Grid.Column="0"
                Height="50"
                Width="100">
            Book Now
        </Button>

        <Button Grid.Column="1"
                Height="50"
                Width="100">
            View Reservations
        </Button>

        <Button Command="{Binding OpenHotelManagementViewCommand}"
                Grid.Column="2"
                Height="50"
                Width="150">
            Hotels Management
        </Button>
    </Grid>
</Window>
